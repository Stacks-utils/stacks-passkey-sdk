# Relay Admin (legacy)

**Use the unified portal instead:** `npm run dev:demo` → http://localhost:3000/portal

This standalone package is kept for reference. Admin functionality lives in `examples/demo/src/admin/`.
